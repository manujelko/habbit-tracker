{% extends "layout.html.j2" %}

{% block main_content %}
    <section class="habit-list">
        {% for habit in habits %}
            <div class="habit">
                <p class="habit__name">
                    {{ habit }}
                </p>
            </div>
        {% endfor %}
    </section>
{% endblock %}